export const perUrl = "/api";
/**
 * 上传教学任务
 * @type {string}
 */
export const uploadTeachTaskUrl = perUrl + "/teacherCourse/excel/teachTask";

/**
 * 上传老师信息
 * @type {string}
 */
export const uploadTeacherUrl = perUrl + "/teacher/excel";

/**
 * 上传学生课程分数url
 * @type {string}
 */
export const uploadTeachCourScore = perUrl + "/teacherCourse/excel/teachCourScore";


/**
 * 上传学生信息(模拟生成)
 * @type {string}
 */
export const uploadStudent = perUrl + "/student/excel";

/**
 * 上传专业信息
 * @type {string}
 */
export const uploadProfession = perUrl + "/profession/excel";

/**
 * 上传学院信息
 * @type {string}
 */
export const uploadFaculty = perUrl + "/faculty/excel";

/**
 * 上传课程信息
 * @type {string}
 */
export const uploadCourse = perUrl + "/course/excel";

/**
 * 上传班级信息
 * @type {string}
 */
export const uploadClass = perUrl + "/class/excel";
